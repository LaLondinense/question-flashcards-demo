function flipCard(card) {
  card.classList.toggle('flipped');
}

function playAudio(filename) {
  const audio = new Audio('audio/' + filename);
  audio.play();
body.appendParagraph("----- Card " + i + " -----");
}